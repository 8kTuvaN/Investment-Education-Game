1. 流程图

游戏开始
│
├── A1. 初始化阶段
│   ├─ 生成行业池（tech / medicine / energy / consumption / real_estate）
│   ├─ 随机设定各行业属性：
│   │     • 回合增长率 1%，随机数1.5%
│   ├─ 每个行业生成3个“虚拟股票”
│   ├─ 玩家获得初始资金（$100,000）
│   ├─ 进入主循环（共12个回合）
│
└──→ 进入 B. 回合循环
回合开始 (Round i)
│
├── B1. 抽取市场事件
│   ├─ 个随机事件从事件库抽出
│   ├─ 每个事件有：
│   │     • 类型（宏观*30/行业*30）
│   │     • 短期涨跌影响 下一回合
│   │     • 增长率影响 
│   ├─ 显示“市场新闻”界面 → 告知玩家事件描述
│
├── B2. 玩家决策阶段
│   ├─ 玩家查看当前资产、股票价格、事件信息
│   ├─ 可执行操作：
│   │     • 买入股票（指定数量）
│   │     • 卖出股票
│   ├─ 玩家确认操作后 → 进入结算阶段
│
├── B3. 市场价格更新
│   ├─ 对每支股票计算：
│   │     新价格 = 旧价格 × (1 + 增长率 + 事件影响 + 随机波动)
│   ├─ 更新市场价格
│   ├─ 更新长期趋势（如果事件影响延续）
│
├── B4. 玩家资产更新
│   ├─ 计算：
│   │     总资产 = 持仓价值 + 现金
│
└──→ 进入下一回合 (Round i+1)
      │
      └─ 若 Round = N → 进入结算阶段

游戏结束（第N回合）
│
├── 资产计算
│   ├─ 总资产
│   ├─ 平均收益率
└──→ 进入 D. 复盘 / 再来一局

2. 编程架构

stock_game/
│
├── main.py             # 程序入口（控制主循环）
│
├── config.py        # 全局参数设置（）
│
├── data_manager.py       # 股票与行业数据结构定义（生成、更新价格）
│
├── event.py            # 市场事件逻辑（事件库、触发与影响计算）
│
├── player.py             # 玩家类（资金、持仓、买卖操作、资产更新）
│
├── ui.py             # 命令行或界面显示逻辑（市场新闻、价格表、玩家资产）
│
└── utils.py             # 通用函数（随机数、格式化输出、日志记录）
